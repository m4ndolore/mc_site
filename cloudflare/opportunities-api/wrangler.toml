name = "opportunities-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
SBIR_API_URL = "https://sbir.mergecombinator.com"
STAGEHAND_URL = "https://mc-stagehand-tests.defensebuilders.workers.dev"
# IRREGULARS_API_TOKEN — set via `wrangler secret put IRREGULARS_API_TOKEN`
# When set, /api/intel/feed uses authenticated Irregulars API; otherwise falls back to public HTML scrape
