# Cloudflare Pages redirects
# Format: /source /destination [status]

# Note: Cloudflare Pages automatically serves .html files without extension
# Only add redirects here for non-standard routing (external URLs, etc.)
# DO NOT add rewrites like "/builders /builders.html 200" - this causes redirect loops
