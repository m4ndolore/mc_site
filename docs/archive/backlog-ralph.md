# Ralph Backlog — C2UX Follow-ons (2026-01-27)

## REQ-C2UX-008 — Global Light/Dark Mode System
- Add a theme toggle in the header.
- Respect `prefers-color-scheme` on first load.
- Persist the choice in `localStorage`.
- Provide operational palettes for both themes.
- Target files: `styles.css`, `script.js`, header markup across `*.html`.

## REQ-C2UX-009 — SigmaBlox Modal Refactor (Builders + Access)
- Apply the SigmaBlox “command plane” modal treatment.
- Sharp edges, framed rows, operational typography hierarchy.
- Unify modal controls and buttons to command semantics.
- Target files: `styles/builders.css`, `js/builders/components.js`, `access.html`.

