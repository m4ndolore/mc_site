# SITREP — mc_site — 2026-07-01

Session handoff. Focus this session: **AEO/AI-search enablement, analytics feedback loop, and the mc-content → /signals presentation bridge.**

## Headline status

- ✅ Knowledge-section AEO overhaul: **complete and merged** (PRs #38–48).
- ✅ AI-traffic analytics: **live in production** (edge worker deployed).
- ✅ AI crawler block: **found and fixed** — Cloudflare was 403-ing GPTBot/ClaudeBot/PerplexityBot site-wide; owner disabled "Block AI Bots". AI Labyrinth assessed safe to leave on.
- ✅ Agent discovery: RFC 8288 Link headers merged (#53).
- ✅ mc-content bridge: `promote-signal` scaffolder + integration plan merged (#54).
- ⏳ **One open PR awaiting human approval: #55** (see below).

## ⚠️ Open item — PR #55 (needs your review)

**"Drones Were the First Chapter"** — first real run of the promote-signal pipeline (promotes mc-content `draft-028`, cheap-missiles-vs-drones thesis, into a `/signals` article).

- **State:** OPEN, never merged (correctly held for your approval). Branch `content/signal-cheap-missiles` (remote `3a0d439`).
- **Investigated 2026-07-01:** the article was reported "cut from worktree." It was **not lost** — it's simply not on `main` because the PR was never merged. The file is intact and recoverable on its branch.
- **Merge caveat:** the branch is **11 behind / 20 ahead** of main and touches `vite.config.js` + `public/data/signals.json`, both of which main has since changed (Plausible plugin, other signals). **Expect merge conflicts on those two files** — rebase before merging.
- **Open TODO in the PR:** no hero image yet (intentionally omitted so nothing 404s; feed uses placeholder, og:image falls back to logo). Add art to `/assets/signals/` and restore the `<figure>` when merging.
- **Your call:** review the copy/voice, confirm it complements (not contradicts) the existing interceptor/JIATF signals, add a hero image, resolve conflicts, merge.

## Shipped this session (merged)

| PR | What |
|----|------|
| #38 | Learn book cover crop fix + 50% larger |
| #39–40 | Surfaced orphaned knowledge guides (First Principles, Defense Venture Studio) |
| #41 | Structured data (HowTo + Article) for AI search |
| #42 | Fixed SBIR page hero showing wrong topic |
| #43 | AEO/SEO audit report (`docs/seo-aeo-audit-2026-06-18.md`) |
| #44 | Repaired stale internal "Related" links → topic cluster |
| #45 | Build-inject static content on JS-only category pages (biggest AEO win) |
| #46 | AEO follow-ups (schema, visible dates, meta, crawler links) |
| #47 | FAQ sections + FAQPage schema on category pages |
| #48 | Hero/section margin alignment fix |
| #51 | Edge-level AI traffic analytics (`mc_ai_traffic` AE dataset) — **deployed** |
| #52 | Doc: Cloudflare AI-bot block finding (now resolved) |
| #53 | RFC 8288 agent-discovery Link headers |
| #54 | promote-signal scaffolder + mc-content integration plan |

(Also #50 residency forms, #56 anymouse widget — adjacent work by others / this session.)

## Key state to know

- **Repo:** on `main`, clean except build-regenerated files (builders/dashboard html, companies json, llms.txt, signals.rss, sitemap.xml) — these regenerate on `npm run build`, don't hand-edit.
- **Uncommitted:** `curriculum/src/main.tsx` is modified in the worktree — **in-progress curriculum work, not mine; left untouched.** Also an untracked `docs/plans/2026-06-23-guided-curriculum.md` (not mine).
- **AI analytics:** query via Cloudflare AE — see `docs/ai-traffic-analytics.md`. Watch for rising `crawl` then `referral` events to confirm AI citations now that the block is lifted.
- **Worker deploys:** `wrangler deploy --env=""` from repo root deploys mc-router (production). Wrangler is authed via `CLOUDFLARE_API_TOKEN`.

## Recommended next steps

1. **Review + merge PR #55** (rebase for conflicts, add hero image).
2. **Watch AI-traffic analytics** over the coming weeks — first real read on whether the AEO work is earning citations.
3. **Promote more mc-content drafts** to /signals using the now-proven workflow (`docs/promote-signal-workflow.md`) — candidates noted: draft-026 (FY27 drone budget), draft-027 (private capital reshaping defense).
4. Deferred/declined: DNS-AID records (out of scope, early IETF draft); full data-driven /signals rebuild (premature).

## Reference docs created this session

- `docs/seo-aeo-audit-2026-06-18.md` — the audit + roadmap
- `docs/ai-traffic-analytics.md` — analytics schema + query guide
- `docs/URGENT-ai-bots-blocked.md` — the (now resolved) CF bot-block finding
- `docs/mc-content-integration-options.md` — integration analysis
- `docs/promote-signal-workflow.md` — how to promote a draft → signal
